{
  "description": "Quiz developers on git commit/push operations",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/scripts/check-git-op.sh"
          }
        ]
      }
    ]
  }
}
